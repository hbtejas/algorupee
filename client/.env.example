# Required — points to Vercel proxy or your backend directly
VITE_API_URL=/

# Required — WebSocket backend URL (same as your backend host)
VITE_WS_URL=https://your-backend.onrender.com

# Optional — only needed for local dev pointing at local backend
# VITE_BACKEND_URL=http://localhost:5000
